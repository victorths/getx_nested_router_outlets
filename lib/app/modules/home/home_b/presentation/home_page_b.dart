import 'package:flutter/material.dart';

class HomePageB extends StatelessWidget {
  const HomePageB({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Home Page B'),
      ),
    );
  }
}
